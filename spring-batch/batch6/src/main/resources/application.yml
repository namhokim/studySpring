spring:
  application:
    name: batch6
  datasource:
    url: jdbc:mysql://localhost:3306/batch6
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  batch:
    jdbc:
      initialize-schema: always
